import { DISHES } from '../shared/dishes';

//educer function takes state and action as parameters
export const Dishes = (state = DISHES, action) => {
    switch(action.type) {
        default:
            return state
    }
}